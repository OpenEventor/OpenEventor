# SplitsPage

## Purpose
Displays computed timing results (splits). Currently a placeholder stub.

## File
- `SplitsPage.tsx`

## Route
`/events/:eventId/splits`

## Future
- Fetch results from `GET /api/events/:eventId/results`
- Display split times per checkpoint
- Filter by course and group
- Real-time updates via SSE

## Dependencies
- None (placeholder)
